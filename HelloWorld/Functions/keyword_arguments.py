# here is a function that increments a number by a given value
def increment(number, by):
    return number + by


# to make the code more readable we can prefix it with the paramarter name
print(increment(2, by=1))